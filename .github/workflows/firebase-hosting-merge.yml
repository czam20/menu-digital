# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
env:
  VITE_BACKEND_BASE_URL: ${{secrets.VITE_BACKEND_BASE_URL}}
  VITE_CLOUDINARY_URL: ${{secrets.VITE_CLOUDINARY_URL}}
  VITE_CLOUDINARY_CLOUD_NAME: ${{secrets.VITE_CLOUDINARY_CLOUD_NAME}}
  VITE_CLOUDINARY_API_KEY: ${{secrets.VITE_CLOUDINARY_API_KEY}}
  VITE_CLOUDINARY_API_SECRET: ${{secrets.VITE_CLOUDINARY_API_SECRET}}
  VITE_CLOUDINARY_UPLOAD_PRESET: ${{secrets.VITE_CLOUDINARY_UPLOAD_PRESET}}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MENU_DIGITAL_83A55 }}
          channelId: live
          projectId: menu-digital-83a55
